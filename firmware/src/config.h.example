#ifndef CONFIG_H
#define CONFIG_H

#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"

#define DHT_PIN D4
#define DHT_TYPE DHT22

#define WIFI_CONNECT_TIMEOUT_MS 15000

#define SUPABASE_URL "https://xxxxxxxxxxxx.supabase.co"
// service_role key (Project Settings > API > "secret" key), NOT the anon key.
// Bypasses RLS, so keep config.h out of git (already gitignored) and never
// paste this key into dashboard-web or any other public/client-side file.
#define SUPABASE_SERVICE_KEY "your-service-role-key"
#define SUPABASE_TABLE "readings"

#define DEEP_SLEEP_SECONDS 300

#endif
